# Style AI — marketing site

Static landing page built with Vite and Tailwind CSS.

```bash
npm install
npm run dev
```

Production build output is written to `dist/` (`npm run build`, then `npm run preview`).
